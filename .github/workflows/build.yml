name: Build
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Build
      run: make upload-dodo upload-jepsen-runner upload-klogdump upload-stmtflow upload-testexec
